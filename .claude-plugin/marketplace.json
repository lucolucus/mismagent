{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "mismagent-method",
  "description": "mismAgent's local marketplace — agentic flow in three movements (explore -> model -> build), portable (generic core + per-project profile). One plugin; the weight scales with the case.",
  "owner": {
    "name": "Luca Parsani"
  },
  "plugins": [
    {
      "name": "mismagent",
      "description": "mismAgent: dialogic explore (challenger/analyst), tactical model -> building-block manifest (types pinned at the boundaries), realize-* workers per block type, worker-composer (merge=composition, D2 on the boundary).",
      "source": "./plugins/mismagent",
      "category": "development"
    }
  ]
}
